Для ініціалізації репозиторія робимо git init

Щоб зрощуміти стан репозиторію на данний момент - git status

Щоб додати файл в staging area команда - git add<file_name>

// comment from another developer

для того щоб створити новий бранч з того на якому ми знаходимося, пишем - git checkout -b <new_branch_name>

from branch feature a

from feature branch b
